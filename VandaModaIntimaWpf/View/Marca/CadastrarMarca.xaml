<Window x:Class="VandaModaIntimaWpf.View.Marca.CadastrarMarca"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VandaModaIntimaWpf.View.Marca"
        mc:Ignorable="d"
        Title="Cadastrar Marca"
        Height="150"
        Width="500"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Icon="/Resources/Marca_Icon.ico">
    <Grid Margin="15, 15, 15, 0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Label>Nome:</Label>
        <TextBox Grid.Column="1"
                 Text="{Binding Marca.Nome}" />
        <Button Grid.Row="1"
                Grid.ColumnSpan="2"
                Style="{StaticResource BotaoCadastrar}"
                IsEnabled="{Binding IsEnabled}"
                Command="{Binding CadastrarComando}">Cadastrar</Button>
    </Grid>
</Window>
